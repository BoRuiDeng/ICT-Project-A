<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Healthcare Portal - MFA</title>
    <style>
        :root {
            --primary: #6C5DD3;
            --primary-dark: #5648B1;
            --background-dark: #1C1C28;
            --background-light: #28293D;
            --text-light: #FFFFFF;
            --text-gray: #8E8EA9;
            --border-color: #363751;
            --danger-color: #E74C3C;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: var(--background-dark);
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .mfa-container {
            background: var(--background-light);
            border-radius: 12px;
            padding: 2rem;
            width: 400px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .mfa-header {
            margin-bottom: 1.5rem;
        }

        .mfa-header h1 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .mfa-header p {
            color: var(--text-gray);
            font-size: 0.9rem;
        }

        .mfa-question {
            margin-bottom: 1.5rem;
            font-size: 1rem;
            font-weight: 500;
        }

        .mfa-input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            background: var(--background-dark);
            color: var(--text-light);
            margin-bottom: 1.5rem;
            font-size: 1rem;
        }

        .mfa-input::placeholder {
            color: var(--text-gray);
        }

        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: var(--primary);
            color: var(--text-light);
            border: none;
            border-radius: 8px;
            text-decoration: none;
            text-align: center;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background: var(--primary-dark);
        }

        .error-message {
            color: var(--danger-color);
            font-size: 0.9rem;
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="mfa-container">
        <!-- MFA Header -->
        <div class="mfa-header">
            <h1>Multi-Factor Authentication</h1>
            <p>Please scan the QR code to verify your identity.</p>
        </div>
    
        <!-- QR Code Placeholder -->
        <div class="qr-code-placeholder">
            <img src="path/to/qr-code-placeholder.png" alt="Scan this QR code" style="width: 200px; height: 200px;">
            <p>Scan this QR code with your authenticator app to proceed.</p>
        </div>
    
        <!-- Submit Button -->
        <button class="btn" onclick="verifyQRCode()">Submit</button>
    
        <!-- Error Message -->
        <div class="error-message" id="error-message" style="display: none;">
            QR code verification failed. Please try again.
        </div>
    </div>
</body>
</html>
