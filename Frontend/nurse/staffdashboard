<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff Dashboard</title>
    
     <!-- Link to Bootstrap CSS -->
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
     <!-- Link to CDN icons -->
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        :root {
            --primary: #6C5DD3;
            --primary-dark: #5648B1;
            --primary-light: #8677E9;
            --background-dark: #1C1C28;
            --background-light: #28293D;
            --text-light: #FFFFFF;
            --text-gray: #8E8EA9;
            --border-color: #363751;
            --success-color: #2ECC71;
            --warning-color: #F1C40F;
            --danger-color: #E74C3C;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: var(--background-dark);
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
        }

        /* Sidebar */
        .vertical-nav {
            height: 100vh; /* Full height */
            width: 250px; /* Fixed width for navbar */
            background-color: #000000; /* Dark background */
            color: white;
            padding-top: 20px;
        }
        .vertical-nav a {
            display: block; /* Ensures each link is on its own line */
            color: white;
            text-decoration: none;
            padding: 10px 15px;
            margin-bottom: 5px;
        }
        .vertical-nav a:hover {
            background-color: rgba(78, 79, 235, 0.5); /* 0.5 controls transparency */
            padding: 10px 55px;
            text-decoration: none;
        }


        /* Main Content */
        .main-content {
            margin-left: 100px;
            padding: 2rem;
            width: calc(100% - 250px);
        }

        /* Profile Summary */
        .profile-summary {
            background: var(--background-light);
            border-radius: 12px;
            padding: 2rem;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .profile-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: var(--primary);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
        }

        .profile-info h2 {
            margin-bottom: 0.5rem;
        }

        .profile-info p {
            color: var(--text-gray);
        }

        /* Dashboard Grid */
        .dashboard-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
        }

        /* Activity Feed */
        .activity-feed {
            background: var(--background-light);
            border-radius: 12px;
            padding: 1.5rem;
        }

        .activity-item {
            padding: 1rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .activity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .activity-icon.success {
            background: rgba(46, 204, 113, 0.2);
            color: var(--success-color);
        }

        .activity-icon.warning {
            background: rgba(241, 196, 15, 0.2);
            color: var(--warning-color);
        }

        /* Notifications */
        .notifications {
            background: var(--background-light);
            border-radius: 12px;
            padding: 1.5rem;
        }

        .notification-item {
            padding: 1rem;
            border-bottom: 1px solid var(--border-color);
        }

        .notification-item:last-child {
            border-bottom: none;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .badge {
            background: var(--primary);
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.875rem;
        }

        @media (max-width: 1024px) {
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav>
        <div class="vertical-nav d-flex flex-column align-items-start">
                <a class="navbar-brand d-flex justify-content-center align-items-center rounded mx-3" 
                style="background-color: rgb(78, 79, 235); padding: 10px 85px;" href="#">FUND</a>
                <br>
                <a class="d-flex justify-content-start align-items-center rounded mx-3 " href="#home">
                    <i class="bi bi-house me-2"></i>Home
                  </a>
            <a class="d-flex justify-content-start align-items-center rounded mx-3 " href="#home">
                <i class="bi bi-border me-2"></i>Dashboard
              </a>
              <a class="d-flex justify-content-start align-items-center rounded mx-3 " href="#home">
                <i class="bi bi-journal-plus me-2"></i>Service Management
              </a>
              <a class="d-flex justify-content-start align-items-center rounded mx-3" href="#about">
                <i class="bi bi-receipt me-2"></i>Billing
              </a>
              <a class="d-flex justify-content-start align-items-center rounded mx-3" href="#services">
                <i class="bi bi-archive me-2"></i>Inventory
              </a>
              <a class="d-flex justify-content-start align-items-center rounded mx-3" href="#services">
                <i class="bi bi-box-arrow-right me-2 "></i> Sign out
              </a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Profile Summary -->
        <div class="profile-summary">
            <div class="profile-avatar">JD</div>
            <div class="profile-info">
                <h2>John Doe</h2>
                <p>Staff ID: #12345</p>
                <p>Role: Staff</p>
            </div>
        </div>

        <!-- Dashboard Grid -->
        <div class="dashboard-grid">
            <!-- Activity Feed -->
            <div class="activity-feed">
                <div class="section-header">
                    <h3>Recent Activity</h3>
                    <span class="badge">3 New</span>
                </div>
                <div class="activity-item">
                    <div class="activity-icon success">✓</div>
                    <div>
                        <h4>Inventory Updated</h4>
                        <p>New medical supplies added.</p>
                    </div>
                </div>
                <div class="activity-item">
                    <div class="activity-icon warning">⚠</div>
                    <div>
                        <h4>Service Request Pending</h4>
                        <p>Request #456 is awaiting approval.</p>
                    </div>
                </div>
            </div>

        </div>
    </div>
</body>
</html>
